import { Clock3 } from "lucide-react";
import type { AthletePerformance } from "../types/athlete.types";
import { formatDate, formatEventName, formatTime } from "../lib/formatters";

export function TimeHistory({ performances }: { performances: AthletePerformance[] }) {
  return <section><div className="mb-5"><p className="text-xs uppercase tracking-[0.2em] text-accent">Resultados</p><h2 className="mt-1 text-3xl font-black uppercase text-white" style={{ fontFamily: "'Barlow Condensed', sans-serif" }}>Historial de tiempos</h2></div>{performances.length === 0 ? <div className="rounded-xl border border-dashed border-white/15 p-8 text-center text-sm text-muted-foreground">Aún no hay tiempos publicados.</div> : <div className="overflow-hidden rounded-xl border border-white/10"><div className="hidden grid-cols-[1.3fr_1fr_0.7fr_0.5fr] gap-4 bg-secondary/80 px-4 py-3 text-[10px] font-bold uppercase tracking-widest text-muted-foreground sm:grid"><span>Prueba</span><span>Competencia</span><span>Marca</span><span>Lugar</span></div>{performances.map((performance) => <div key={performance.id} className="grid gap-3 border-b border-white/10 bg-card/50 p-4 last:border-0 sm:grid-cols-[1.3fr_1fr_0.7fr_0.5fr] sm:items-center sm:gap-4 sm:px-4 sm:py-3"><div><div className="flex items-center gap-2 text-sm font-semibold text-white"><Clock3 size={14} className="text-accent" /> {formatEventName(performance)}</div><p className="mt-1 text-xs text-accent">{performance.swimType} · {performance.course}</p></div><div className="text-xs text-muted-foreground">{performance.competitionName}<br />{formatDate(performance.date)}</div><div className="text-xl font-black text-white" style={{ fontFamily: "'Barlow Condensed', sans-serif" }}>{formatTime(performance.timeMs)}</div><div className="text-sm font-bold text-accent">#{performance.place}</div></div>)}</div>}</section>;
}
