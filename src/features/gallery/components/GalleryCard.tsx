import { Play } from "lucide-react";
import type { GalleryItem } from "../types/gallery.types";
export function GalleryCard({ item }: { item: GalleryItem }) { return <article className="overflow-hidden rounded-2xl border border-white/10 bg-card/70"><div className="relative aspect-[4/3] overflow-hidden bg-secondary"><img src={item.src} alt={item.alt} className="h-full w-full object-cover transition duration-500 hover:scale-105" />{item.type === "video" && <span className="absolute left-3 top-3 inline-flex items-center gap-1 rounded-full bg-accent px-2 py-1 text-[10px] font-black uppercase text-[#061529]"><Play size={11} fill="currentColor" /> Video</span>}</div><div className="p-4"><div className="flex items-center justify-between gap-3 text-[10px] uppercase tracking-widest text-accent"><span>{item.date}</span><span>{item.author}</span></div><h2 className="mt-2 text-2xl font-black uppercase leading-none text-white" style={{ fontFamily: "'Barlow Condensed', sans-serif" }}>{item.title}</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">{item.description}</p><p className="mt-3 text-xs text-white/50">{item.competitionName}</p></div></article>; }
