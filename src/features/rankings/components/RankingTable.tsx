import Link from "next/link";
import { ROUTES } from "@/features/navigation";
import { formatTime } from "@/features/athletes/lib/formatters";
import type { RankingEntry } from "../types/ranking.types";

export function RankingTable({ entries }: { entries: RankingEntry[] }) { return <div className="overflow-hidden rounded-2xl border border-white/10"><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-sm"><thead className="bg-secondary text-left text-[10px] uppercase tracking-widest text-muted-foreground"><tr>{["#", "Atleta", "Club", "Prueba", "Piscina", "Tiempo", "Puntos", "Competencia", "Fecha"].map((heading) => <th key={heading} className="px-4 py-3">{heading}</th>)}</tr></thead><tbody>{entries.map((entry) => <tr key={entry.id} className="border-t border-white/5 hover:bg-card"><td className="px-4 py-3 font-black text-accent">{entry.position}</td><td className="px-4 py-3"><Link href={ROUTES.atletaDetalle(entry.athleteId)} className="font-semibold text-white hover:text-accent">{entry.athleteName}</Link><span className="block text-xs text-muted-foreground">{entry.category}</span></td><td className="px-4 py-3"><Link href={ROUTES.clubDetalle(entry.clubId)} className="text-white/70 hover:text-accent">{entry.clubName}</Link></td><td className="px-4 py-3 text-white/70">{entry.distanceMeters} m {entry.stroke}<span className="block text-xs text-muted-foreground">{entry.discipline}</span></td><td className="px-4 py-3 text-white/60">{entry.course}</td><td className="px-4 py-3 font-black text-white">{formatTime(entry.timeMs)}</td><td className="px-4 py-3 font-black text-accent">{entry.points}</td><td className="px-4 py-3 text-white/60">{entry.competitionName}<span className="block text-xs text-muted-foreground">{entry.location}</span></td><td className="px-4 py-3 whitespace-nowrap text-xs text-muted-foreground">{entry.date}</td></tr>)}</tbody></table></div>{entries.length === 0 && <div className="px-6 py-16 text-center text-sm text-muted-foreground">No se encontraron marcas con los filtros seleccionados.</div>}</div>; }
