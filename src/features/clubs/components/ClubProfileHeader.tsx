import Link from "next/link";
import { ArrowLeft, MapPin, ShieldCheck } from "lucide-react";
import { ROUTES } from "@/features/navigation";
import { getClubStats } from "../lib/club-stats";
import type { Club } from "../types/club.types";

export function ClubProfileHeader({ club }: { club: Club }) { const stats = getClubStats(club); return <div className="relative overflow-hidden rounded-2xl border border-white/10 bg-card/80 p-6 sm:p-8"><div className="absolute -right-12 -top-16 h-56 w-56 rounded-full bg-accent/10 blur-3xl" /><Link href={ROUTES.clubes} className="relative inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-accent hover:text-white"><ArrowLeft size={14} /> Volver a clubes</Link><div className="relative mt-8 flex flex-col gap-5 sm:flex-row sm:items-center"><div className="flex h-24 w-24 items-center justify-center rounded-2xl bg-gradient-to-br from-primary to-accent/20 text-3xl font-black text-accent">{club.shortName}</div><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.18em] text-accent"><ShieldCheck size={14} /> Club afiliado · {club.federationCode}</div><h1 className="mt-2 text-5xl font-black uppercase leading-none text-white sm:text-6xl" style={{ fontFamily: "'Barlow Condensed', sans-serif" }}>{club.name}</h1><p className="mt-3 flex items-center gap-2 text-white/70"><MapPin size={15} className="text-accent" /> {club.city}, {club.department} · Desde {club.foundedYear}</p></div></div><div className="relative mt-8 grid grid-cols-2 gap-3 sm:grid-cols-4"><Stat value={stats.activeAthletes} label="Atletas actuales" /><Stat value={stats.totalPoints} label="Puntos" /><Stat value={stats.totalMedals} label="Medallas" /><Stat value={stats.competitionsCount} label="Competencias" /></div></div>; }
function Stat({ value, label }: { value: number; label: string }) { return <div className="rounded-xl border border-white/10 bg-secondary/50 p-3"><p className="text-3xl font-black text-white" style={{ fontFamily: "'Barlow Condensed', sans-serif" }}>{value}</p><p className="text-[10px] uppercase tracking-widest text-muted-foreground">{label}</p></div>; }
