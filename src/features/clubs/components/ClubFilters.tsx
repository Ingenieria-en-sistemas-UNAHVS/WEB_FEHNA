"use client";

import { Search } from "lucide-react";
import type { ClubSort } from "../types/club.types";

export function ClubFilters({ search, sort, onSearch, onSort }: { search: string; sort: ClubSort; onSearch: (value: string) => void; onSort: (value: ClubSort) => void }) { return <div className="grid gap-3 rounded-2xl border border-white/10 bg-card/70 p-4 sm:grid-cols-[1fr_220px]"><label className="relative"><span className="sr-only">Buscar club</span><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Buscar por club, ciudad o departamento" className="h-11 w-full rounded-lg border border-white/10 bg-secondary pl-9 pr-3 text-sm text-white placeholder:text-muted-foreground focus:border-accent focus:outline-none" /></label><label><span className="sr-only">Ordenar clubes</span><select value={sort} onChange={(event) => onSort(event.target.value as ClubSort)} className="h-11 w-full rounded-lg border border-white/10 bg-secondary px-3 text-sm text-white focus:border-accent focus:outline-none"><option value="points">Más puntos</option><option value="medals">Más medallas</option><option value="athletes">Más atletas</option><option value="competitions">Más competencias</option><option value="name">Orden alfabético</option></select></label></div>; }
